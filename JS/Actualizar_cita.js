function editarRegistro(id) {
    // Opción 1: Redirigir a otra página con el ID del registro en la URL
    window.location.href = '../Agente/Actualizar.php?id=' + id;
}

function evidencia(id) {
    // Opción 1: Redirigir a otra página con el ID del registro en la URL
    window.location.href = '../Agente/Evidencia.php?id=' + id;
}
